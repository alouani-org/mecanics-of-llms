# 🤖 دمج وكلاء ReAct

🌍 [English](../en/REACT_AGENT_INTEGRATION.md) | 📖 [Français](../fr/REACT_AGENT_INTEGRATION.md) | 🇪🇸 [Español](../es/REACT_AGENT_INTEGRATION.md) | 🇧🇷 [Português](../pt/REACT_AGENT_INTEGRATION.md) | 🇸🇦 **العربية**

## 📍 التنقل السريع

- **📖 اقرأ أولاً:** [PEDAGOGICAL_JOURNEY.md](./PEDAGOGICAL_JOURNEY.md) - أين تندمج الوكلاء
- **⚡ البدء السريع:** [QUICKSTART_SCRIPT_09.md](./QUICKSTART_SCRIPT_09.md) - تشغيل السكريبت 06

---

## 🎯 ما هو وكيل ReAct؟

**ReAct** = **Re**asoning (التفكير) + **Act**ing (التصرف)

وكيل يقوم بـ:
1. **التفكير** (Reasoning) - تحليل المشكلة
2. **التصرف** (Acting) - استخدام أداة
3. **الملاحظة** (Observe) - تلقي النتيجة
4. **الحلقة** (Loop) - التكرار حتى الإجابة

### مثال: الإجابة على سؤال معقد

```
السؤال: "ما هي عاصمة البلد الأكثر سكانًا؟"

الوكيل يفكر:
"يجب أن أجد أولاً البلد الأكثر سكانًا"

الوكيل يتصرف:
يستخدم أداة "search" ← "الهند والصين"

الوكيل يلاحظ:
"كلاهما لديه ~1.4 مليار نسمة"

الوكيل يفكر:
"الهند حاليًا الأكثر سكانًا"

الوكيل يتصرف:
يستخدم أداة "get_capital" ← "نيودلهي"

الوكيل يلاحظ:
"الإجابة هي نيودلهي"

الوكيل: "عاصمة البلد الأكثر سكانًا هي نيودلهي"
```

---

## 🏗️ بنية حلقة ReAct

```
┌─────────────────────┐
│  السؤال الأولي      │
└──────────┬──────────┘
           │
    ┌──────▼──────┐
    │ THINK       │ ← تحليل الحالة
    │ (التفكير)   │
    └──────┬──────┘
           │
    ┌──────▼──────┐
    │ ACT         │ ← اختيار أداة
    │ (التصرف)    │
    └──────┬──────┘
           │
    ┌──────▼──────────┐
    │ OBSERVE         │ ← معالجة النتيجة
    │ (الملاحظة)      │
    └──────┬──────────┘
           │
    منته؟ │
    ┌──────┴─────┐
    │ نعم │ لا   │
    │     │      │ → العودة إلى THINK
    │     │      │
    ▼     ▼
 الإجابة الحلقة
```

---

## 🛠️ الأدوات في السكريبتات 06 و 09

### الأداة 1: Calculator (الحاسبة)
```python
def tool_calculator(expression: str) -> str:
    """إجراء الحسابات الرياضية"""
    try:
        result = eval(expression)
        return f"النتيجة: {result}"
    except:
        return "خطأ في الحساب"

# الاستخدام من قبل الوكيل:
# الفعل: calculator[2 + 2]
# ← النتيجة: 4
```

### الأداة 2: Search (البحث)
```python
def tool_search(query: str) -> str:
    """البحث في قاعدة المعرفة"""
    # مُنفّذ عبر RAGSystem
    results = rag.retrieve(query, top_k=3)
    return format_results(results)

# الاستخدام من قبل الوكيل:
# الفعل: search[transformer attention mechanism]
# ← النتيجة: وثائق ذات صلة...
```

### الأداة 3: Current Time (الساعة)
```python
def tool_current_time() -> str:
    """إرجاع الوقت الحالي"""
    from datetime import datetime
    return datetime.now().isoformat()

# الاستخدام من قبل الوكيل:
# الفعل: current_time[]
# ← النتيجة: 2025-01-15T14:32:00
```

### الأداة 4: Summarize (التلخيص)
```python
def tool_summarize(text: str) -> str:
    """إنشاء ملخص للنص"""
    lines = text.split(".")
    return ". ".join(lines[:2])  # مُبسّط

# الاستخدام من قبل الوكيل:
# الفعل: summarize[نص طويل...]
# ← النتيجة: ملخص مُكثّف
```

---

## 📋 تنسيق وكيل ReAct

يتواصل الوكلاء بهذا التنسيق المنظم:

```
Thought: ماذا يجب أن أفعل الآن؟
Action: tool_name[param1, param2]
Observation: [نتيجة الأداة]

Thought: الخطوة التالية؟
Action: tool_name[param]
Observation: [النتيجة]

Thought: لدي الإجابة
Final Answer: الإجابة هي...
```

### مثال كامل:

```
Thought: المستخدم يسأل عن عاصمة بلد مكتظ بالسكان.
يجب أن أحدد أولاً البلد الأكثر سكانًا.
Action: search[most populous country]
Observation: الهند لديها 1.42 مليار نسمة، الصين 1.41 مليار.

Thought: الهند هي الأكثر سكانًا. الآن يجب أن أجد عاصمتها.
Action: search[capital of India]
Observation: عاصمة الهند هي نيودلهي.

Thought: لقد حصلت على الإجابة الكاملة.
Final Answer: عاصمة البلد الأكثر سكانًا هي نيودلهي
(الهند مع 1.42 مليار نسمة).
```

---

## 🔄 أنماط اختيار الأدوات

### النمط 1: الاختيار بناءً على الكلمات المفتاحية

```python
if "حساب" in thought.lower():
    use_tool("calculator")
elif "عاصمة" in thought.lower():
    use_tool("search")
elif "وقت" in thought.lower():
    use_tool("current_time")
```

### النمط 2: الاختيار بالنقاط

```python
scores = {}
for tool_name, tool_desc in available_tools.items():
    score = similarity(thought, tool_desc)
    scores[tool_name] = score

best_tool = max(scores, key=scores.get)
```

### النمط 3: الاختيار عبر LLM (متقدم)

```python
# استخدام LLM للاختيار
prompt = f"""
الفكرة: {thought}
الأدوات المتاحة: {available_tools}
أي أداة ستستخدم؟
"""
response = llm(prompt)
selected_tool = parse_tool(response)
```

---

## 🎓 المفاهيم الأساسية

### 1. الاستقلالية
الوكيل يقرر بنفسه ما يجب فعله. لا توجيه بشري خطوة بخطوة.

### 2. التكرار
يمكن للوكيل القيام بعدة خطوات. ليس هناك إجابة مباشرة واحدة.

### 3. الأدوات
الأدوات توسع قدرات الوكيل تتجاوز نموذج اللغة الكبيرة وحده.

### 4. الشفافية
كل خطوة مسجلة ومرئية (الـ "trace").

### 5. التوقف التلقائي
الوكيل يعرف متى انتهى (اكتشاف "Final Answer").

---

## ⚠️ القيود والتحديات

### 1. الهلوسة
قد يخترع الوكيل إجابات حتى مع الأدوات.

**الحل:** التحقق من نتائج الأداة.

### 2. الحلقات اللانهائية
قد يعلق الوكيل في حلقة.

**الحل:** تحديد `max_iterations`.

### 3. اختيار أداة خاطئة
قد يختار الوكيل الأداة الخاطئة.

**الحل:** تحسين أوصاف الأدوات.

### 4. التكلفة
كل أداة = وقت + مال.

**الحل:** تحسين اختيار الأدوات.

---

## 💡 التوسعات العملية

### إضافة أداة جديدة

```python
# 1. تعريف الدالة
def tool_weather(city: str) -> str:
    """استرجاع طقس المدينة"""
    return f"باريس: مشمس، 22°C"

# 2. تسجيلها
agent.tools.register(
    name="weather",
    description="استرجاع الطقس لمدينة",
    function=tool_weather
)

# الآن يمكن للوكيل استخدامها!
# الفعل: weather[Paris]
```

### تحسين اختيار الأدوات

```python
# إضافة أوصاف تفصيلية
tool_descriptions = {
    "calculator": "للحسابات الرياضية: الجمع، الطرح، إلخ.",
    "search": "للعثور على معلومات: مفاهيم، تعريفات",
    "weather": "لظروف الطقس في مدينة",
}

# الوكيل يستخدمها لقرارات أفضل
```

### إضافة الذاكرة

```python
# حفظ القرارات السابقة
class AgentWithMemory:
    def __init__(self):
        self.history = []
    
    def remember(self, action, result):
        self.history.append({"action": action, "result": result})
    
    def recall_similar(self, new_action):
        # البحث عن فعل مشابه في التاريخ
        return [h for h in self.history if similar(h["action"], new_action)]
```

---

## 🎯 حالات الاستخدام

### ✅ مثالي لـ:
- البحث متعدد الخطوات
- حل المشاكل المعقدة
- الأسئلة التي تتطلب مصادر متعددة
- المهام مع أدوات متخصصة

### ❌ غير موصى به لـ:
- الأسئلة البسيطة (بطيء جدًا)
- الوقت الفعلي الحرج (مكلف جدًا)
- القرارات الحرجة جدًا (خطر الهلوسة)

---

## 🚀 التكامل مع نموذج لغة كبيرة حقيقي

### مع OpenAI

```python
from openai import OpenAI

client = OpenAI()

def agent_with_openai(query):
    context = ""
    for iteration in range(3):
        # استخدام GPT للتفكير
        response = client.chat.completions.create(
            model="gpt-4",
            messages=[
                {"role": "user", "content": f"{context}\nالسؤال: {query}"}
            ]
        )
        thought = response.choices[0].message.content
        
        # تحليل الفعل
        tool_name, params = parse_action(thought)
        
        # التنفيذ
        observation = execute_tool(tool_name, params)
        
        context += f"\n{thought}\nالملاحظة: {observation}"
    
    return context
```

---

## 📚 العلاقة مع السكريبت 09

السكريبت 09 يدمج كل هذا:
- **حلقة ReAct** الكاملة
- **4 أدوات** متاحة
- **تقييم** الإجابات
- **استمرارية** الأثر

راجع [QUICKSTART_SCRIPT_09.md](./QUICKSTART_SCRIPT_09.md) للتفاصيل.

---

**مستعد لإنشاء وكلاء مستقلين؟ 🤖**

راجع [القراءة التالية](./LLAMAINDEX_GUIDE.md) لأنظمة RAG الإنتاجية.
