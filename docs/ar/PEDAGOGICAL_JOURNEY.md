# الرحلة التعليمية الكاملة: من الفصل 1 إلى السكريبت 09

🌍 [English](../en/PEDAGOGICAL_JOURNEY.md) | 📖 [Français](../fr/PEDAGOGICAL_JOURNEY.md) | 🇪🇸 [Español](../es/PEDAGOGICAL_JOURNEY.md) | 🇧🇷 [Português](../pt/PEDAGOGICAL_JOURNEY.md) | 🇸🇦 **العربية**

## 📚 نظرة عامة

هذه الوثيقة تُرسم **الرحلة التعليمية الكاملة** عبر الكتاب والسكريبتات العملية، موضحةً كيف يضاف كل مفهوم إلى التالي حتى نبني **المساعد الصغير الكامل (السكريبت 09)**.

---

## المرحلة 1: الأساسيات (الفصول 1-3)

### الهدف: فهم البنية الداخلية لنموذج اللغة الكبيرة

#### الفصل 1: ما هو نموذج اللغة الكبيرة (LLM)؟

**المفاهيم**:
- البنية العامة للمحول (transformer)
- مكدس المشفرات (encoders) والمفككات (decoders)
- حلقة الاستدلال (inference loop)

**السكريبت المرتبط**: لا يوجد (نظري)

---

#### الفصل 2: التجزئة وتمثيل النص

**المفاهيم الأساسية**:
- محللات الرموز (Tokenizers): BPE، WordPiece، Sentencepiece
- معرّفات الرموز (Token IDs) والتضمينات (embeddings)
- طول التسلسل والتكلفة الحسابية

**كود من الكتاب**:
```python
from transformers import AutoTokenizer
tokenizer = AutoTokenizer.from_pretrained("gpt2")
tokens = tokenizer.encode("Bonjour le monde")
print(len(tokens))  # → 4-5 رموز
```

**السكريبت العملي**: [`01_tokenization_embeddings.py`](../../01_tokenization_embeddings.py)

**الرحلة التعليمية**:
1. قم بتشغيل السكريبت
2. افهم تأثير عدد الرموز
3. جرّب محللات رموز مختلفة
4. **الاستبصار**: المزيد من الرموز = تكلفة حسابية أعلى

**التوسع**:
- قارن الفرنسية مقابل الإنجليزية مقابل الصينية
- انظر إلى التأثير على طول التسلسلات

---

#### الفصل 3: بنية المحول (Transformer) والانتباه

**المفاهيم الأساسية**:
- الانتباه الذاتي (Self-attention): الاستعلام (Query)، المفتاح (Key)، القيمة (Value)
- الانتباه متعدد الرؤوس (Multi-head attention)
- أوزان الانتباه (attention weights)
- دور البنية المعمارية

**كود من الكتاب**:
```python
# محاكاة بسيطة للانتباه متعدد الرؤوس
Q = tokens @ W_q
K = tokens @ W_k
V = tokens @ W_v
attention = softmax(Q @ K.T / sqrt(d_k)) @ V
```

**السكريبت العملي**: [`02_multihead_attention.py`](../../02_multihead_attention.py)

**الرحلة التعليمية**:
1. تصور أوزان الانتباه لكل رأس
2. افهم أن رأسًا واحدًا = تبعية واحدة (فاعل-فعل، إلخ)
3. لاحظ كيف "تنظر" كل موضع إلى الآخر
4. **الاستبصار**: الانتباه يلتقط التبعيات اللغوية

**التوسع**:
- أضف الترميز الموضعي (positional encoding)
- تصور في 2D باستخدام t-SNE
- قارن رأس واحد مقابل متعدد الرؤوس

---

## المرحلة 2: التدريب المسبق والتوليد (الفصول 4-7)

### الهدف: فهم كيفية تدريب نماذج اللغة الكبيرة

#### الفصل 4: التدريب المسبق التلقائي التراجعي

**المفاهيم**: التنبؤ بالرمز التالي (next-token prediction)، القناع السببي (causal masking)، الحيرة (perplexity)

**السكريبت**: لا يوجد (تعقيد عالٍ لسكريبت تعليمي)

---

#### الفصول 5-6: المحاذاة والضبط الدقيق

**المفاهيم**: RLHF، DPO، الضبط الدقيق المُشرَف

**السكريبت**: لا يوجد (يتطلب GPU قوي)

---

#### الفصل 7: التدريب المسبق والخسارة

**المفاهيم الأساسية**:
- خسارة الإنتروبيا التقاطعية (Cross-entropy loss)
- الحيرة (Perplexity)
- تأثير درجة الحرارة على التوزيع

**كود من الكتاب**:
```python
logits = model(tokens)
loss = cross_entropy_loss(logits, targets)
perplexity = exp(loss)
```

**السكريبت العملي**: [`03_temperature_softmax.py`](../../03_temperature_softmax.py)

**الرحلة التعليمية**:
1. انظر كيف تغيّر درجة الحرارة التوزيع
2. T=0.1 → حتمي (greedy)
3. T=1.0 → التوزيع الأصلي
4. T=5.0 → شبه موحد (إبداعي)
5. **الاستبصار**: درجة الحرارة = التحكم في الإبداع/الاستقرار

---

## المرحلة 3: التوليد المتقدم (الفصول 8-11)

### الهدف: إتقان استراتيجيات التوليد

#### الفصل 8: تقنيات التوليد

**المفاهيم**: Top-k، Top-p، البحث الشعاعي (Beam search)

**السكريبت**: لا يوجد (مُدمج في الفصل 3)

---

#### الفصل 9: الضبط الدقيق المُشرَف وLoRA

**المفاهيم الأساسية**:
- الضبط الدقيق الكامل مقابل الفعال من حيث المعاملات
- LoRA (التكيف منخفض الرتبة - Low-Rank Adaptation)
- QLoRA (مع التكميم)
- توفير الموارد

**كود من الكتاب**:
```python
# LoRA: إضافة مصفوفات صغيرة BA إلى النموذج
# W = W_0 + BA (rank << dimension)
# فقط BA يتم تدريبها، W_0 مُجمّد
```

**السكريبت العملي**: [`08_lora_finetuning_example.py`](../../08_lora_finetuning_example.py)

**الرحلة التعليمية**:
1. افهم التحليل W = W₀ + BA
2. انظر إلى توفير المعاملات (تخفيض 99%)
3. قارن الضبط الدقيق الكامل مقابل LoRA مقابل QLoRA
4. انظر إلى الحالة الحقيقية (SNCF)
5. **الاستبصار**: LoRA يسمح بتكييف النماذج على GPU استهلاكية

---

#### الفصل 10: الآليات المتقدمة

**المفاهيم**: الانتباه المتفرق (Sparse attention)، قوانين القياس (scaling laws)

**السكريبت**: لا يوجد (نظري)

---

#### الفصل 11: استراتيجيات التوليد والمطالبة (Prompting)

**المفاهيم الأساسية**:
- المطالبة بدون أمثلة (Zero-shot prompting)
- المطالبة مع أمثلة قليلة (Few-shot prompting)
- سلسلة التفكير (Chain-of-Thought - CoT)
- درجة الحرارة والعينات
- معايرة النماذج

**كود من الكتاب**:
```python
# مثال Few-shot
prompt = """
مثال 1: إدخال → إخراج
مثال 2: إدخال → إخراج
سؤال: ...
"""
response = llm(prompt)
```

**السكريبت العملي**: [`03_temperature_softmax.py`](../../03_temperature_softmax.py) (درجة الحرارة)

**الرحلة التعليمية**:
1. جرّب المطالبة في السكريبت 03
2. افهم كيف تتحكم درجة الحرارة في النتيجة
3. انظر إلى الرابط بين درجة الحرارة والاستراتيجية (greedy مقابل sampling)
4. **الاستبصار**: المطالبة = الرافعة الأبسط للتحكم في نموذج اللغة الكبيرة

**التوسع**:
- جرّب تقنيات مطالبة مختلفة
- قارن zero-shot مقابل few-shot مقابل CoT
- قس التأثير على الجودة

**⚠️ المعلم**: أنت تبدأ في فهم **كيفية السؤال** لنماذج اللغة الكبيرة.

---

## المرحلة 4: التقييم (الفصل 12)

### الهدف: قياس وتحسين الجودة

#### الفصل 12: نماذج الاستدلال والتقييم

**المفاهيم الأساسية**:
- Pass@k: احتمال نجاح واحد على الأقل في k محاولة
- Pass^k: احتمال نجاح **جميع** المحاولات
- الاتساق الذاتي (Self-consistency): اتساق الإجابات المتعددة
- المقاييس: BLEU، ROUGE، METEOR، BERTScore
- تقييم الوكلاء

**كود من الكتاب**:
```python
# Pass@k: الصيغة التوافقية
pass_at_k = 1 - (1 - p_success)**k

# الاتساق الذاتي: نفس السؤال، k محاولة
answers = [llm(prompt) for _ in range(k)]
consistency = most_common(answers) / k
```

**السكريبت العملي**: [`05_pass_at_k_evaluation.py`](../../05_pass_at_k_evaluation.py)

**الرحلة التعليمية**:
1. افهم Pass@k (التنوع مقابل الصحة)
2. افهم Pass^k (جميعها صحيحة بشكل صارم)
3. انظر لماذا Pass@k > Pass@1 دائمًا
4. افهم تأثير k
5. **الاستبصار**: Pass@k يلتقط تباين النماذج

**التوسع**:
- نفّذ الاتساق الذاتي
- قارن مع مقاييس أخرى
- قيّم على معيار حقيقي (HumanEval، MMLU)

**✨ المعلم**: يمكنك الآن **تقييم** جودة نموذج اللغة الكبيرة.

---

## المرحلة 5: الأنظمة المعززة (الفصل 13)

### الهدف: تجاوز نموذج اللغة الكبيرة وحده

#### الفصل 13: الأنظمة المعززة وRAG

**المفاهيم الأساسية**:
- RAG: التوليد المعزز بالاسترجاع (Retrieval-Augmented Generation)
- الفهرسة الشعاعية (embeddings)
- الاسترجاع (Retrieval): البحث عن الوثائق ذات الصلة
- التعزيز (Augmentation): حقن السياق في المطالبة
- التوليد (Generation): استخدام السياق للرد

**بنية RAG**:
```
سؤال
    ↓
[الاسترجاع] → أعلى K وثيقة ذات صلة
    ↓
[التعزيز] → السياق + السؤال
    ↓
[التوليد] → الرد بناءً على السياق
```

**كود من الكتاب**:
```python
# RAG مُبسّط
query_embedding = embed(question)
similar_docs = search(query_embedding, db, top_k=3)
augmented_prompt = f"السياق: {similar_docs}\nالسؤال: {question}"
response = llm(augmented_prompt)
```

**السكريبتات العملية**:
- [`04_rag_minimal.py`](../../04_rag_minimal.py): RAG مع TF-IDF
- [`07_llamaindex_rag_advanced.py`](../../07_llamaindex_rag_advanced.py): RAG إنتاجي مع LlamaIndex

**الرحلة التعليمية**:

**المستوى 1 - البسيط**:
1. قم بتشغيل `04_rag_minimal.py`
2. افهم خط الأنابيب: فهرسة → استرجاع → تعزيز
3. انظر كيف يعمل التشابه الجيبي
4. **الاستبصار**: RAG يقلل الهلاوس من خلال الارتكاز على المصادر

**المستوى 2 - المتقدم**:
1. قم بتشغيل `07_llamaindex_rag_advanced.py`
2. اكتشف التقطيع الذكي (chunking)
3. افهم التضمينات الكثيفة مقابل المتفرقة
4. انظر إلى الاستمرارية الحوارية

**التوسع**:
- أضف BM25 (بحث هجين)
- ادمج قاعدة بيانات شعاعية (Pinecone، إلخ)
- نفّذ إعادة الترتيب (reranking)

**✨ المعلم**: لديك الآن **نظام لا يعيد اختراع العجلة**.

---

## المرحلة 6: الوكلاء المستقلون (الفصل 14)

### الهدف: إنشاء نظام يفكر ويتصرف

#### الفصل 14: البروتوكولات القياسية للوكلاء

**المفاهيم الأساسية**:
- نمط ReAct: **Rea**son (التفكير) + **Act** (التصرف)
- الحلقة المستقلة: التفكير ← الفعل ← الملاحظة ← ...
- استدعاء الأدوات (Tool calling): استخدام أدوات خارجية
- بروتوكول سياق النموذج (Model Context Protocol - MCP)
- إدارة التكرارات والأخطاء

**حلقة ReAct**:
```
1. الفكرة (Thought): ماذا يجب أن أفعل؟
2. الفعل (Action): أي أداة أستخدم؟
3. الملاحظة (Observation): ما النتيجة التي حصلت عليها؟
4. [إذا لم أنته] العودة إلى 1
5. [وإلا] الإجابة النهائية
```

**كود من الكتاب**:
```python
# كود وهمي ReAct
for i in range(max_iterations):
    thought = llm.think(context)
    action, params = llm.parse_action(thought)
    observation = tools[action](**params)
    if is_done(thought):
        break
    context.append(observation)
```

**السكريبتات العملية**:
- [`06_react_agent_bonus.py`](../../06_react_agent_bonus.py): وكيل ReAct أساسي
- [`09_mini_assistant_complet.py`](../../09_mini_assistant_complet.py): وكيل كامل (انظر تكملة)

**الرحلة التعليمية**:
1. قم بتشغيل `06_react_agent_bonus.py`
2. انظر إلى حلقة التفكير ← الفعل ← الملاحظة
3. افهم نظام تسجيل الأدوات
4. **الاستبصار**: الوكلاء = حلقة استدلال + تنفيذ

**التوسع**:
- أضف المزيد من الأدوات (طقس، أخبار، API)
- نفّذ إعادة المحاولة مع التراجع الأسّي
- أضف التحقق من المعاملات

**✨ المعلم**: لقد بنيت **نظامًا مستقلاً**.

---

## المرحلة 7: التكامل الكامل (الفصول 11-15 + السكريبت 09)

### 🏆 المشروع المتكامل: المساعد الصغير الكامل

**الهدف**: تجميع **جميع** المفاهيم في نظام متماسك.

#### السكريبت 09: `09_mini_assistant_complet.py`

**ما يجمعه**:

| المفهوم | أين؟ | الفصل |
|---------|------|----------|
| **المطالبة** | `_simulate_llm_reasoning()` | 11 |
| **التقييم** | `AssistantEvaluator` | 12 |
| **RAG** | `RAGSystem` + TF-IDF | 13 |
| **الوكلاء** | `ReActAgent` + الحلقة | 14 |
| **الإنتاج** | إدارة الأخطاء + المراقبة | 15 |

**البنية الكاملة**:

```
سؤال المستخدم
        ↓
   ReActAgent
        ↓
   [الحلقة المستقلة]
   - محاكي LLM
   - سجل الأدوات
   - نظام RAG
        ↓
   [التقييم]
   - الثقة
   - الاتساق الذاتي
        ↓
   [التقرير]
   - التكرارات
   - النجاح
   - الإحصائيات
```

**الرحلة التعليمية**:

1. **التشغيل** `09_mini_assistant_complet.py`
   ```bash
   python 09_mini_assistant_complet.py
   ```

2. **المراقبة** للمراحل الخمس:
   - المرحلة 1: فهرسة قاعدة المعرفة
   - المرحلة 2: إنشاء الوكيل
   - المرحلة 3: معالجة الأسئلة
   - المرحلة 4: التقييم
   - المرحلة 5: اختبار الاتساق الذاتي

3. **التعديل** للتعمق:
   - تغيير الأسئلة
   - إضافة وثائق
   - إضافة أدوات
   - دمج نموذج لغة كبيرة حقيقي

4. **التوسع** للإنتاج:
   - إضافة واجهة ويب
   - استمرار المحادثات
   - تنفيذ تسجيل السجلات (logging)
   - النشر في الإنتاج

**الكود الأساسي للفهم**:

```python
# تهيئة RAG
rag = RAGSystem()
rag.add_document("المحتوى...", {"title": "العنوان"})
rag.index_documents()

# إنشاء الوكيل
agent = ReActAgent(rag_system=rag)
agent.tools.register("calculator", "حسابات", tool_calculator)

# التنفيذ
response = agent.run("كم هو 2+2؟")

# التقييم
evaluator = AssistantEvaluator()
metrics = evaluator.evaluate_response(question, response)
consistency = evaluator.self_consistency_check(agent, question, num_samples=3)
```

---

## 🎓 ملخص الرحلة

```
الفصل 1
     ↓
[مفاهيم نظرية]
     ↓
الفصول 2-3 → السكريبت 01-02 (التجزئة والانتباه)
     ↓
[أنت تفهم البنية الداخلية]
     ↓
الفصول 4-7 → السكريبت 03 (التوليد ودرجة الحرارة)
     ↓
[يمكنك التحكم في التوليد]
     ↓
الفصول 8-9 → السكريبت 08 (الضبط الدقيق مع LoRA)
     ↓
[يمكنك تكييف النماذج]
     ↓
الفصل 11 → المطالبة (نظري)
     ↓
[أنت تعرف كيف تسأل]
     ↓
الفصل 12 → السكريبت 05 (تقييم Pass@k)
     ↓
[يمكنك قياس الجودة]
     ↓
الفصل 13 → السكريبت 04، 07 (RAG)
     ↓
[لديك نظام معزز]
     ↓
الفصل 14 → السكريبت 06 (وكلاء ReAct)
     ↓
[لديك نظام مستقل]
     ↓
الفصل 15 + السكريبت 09 → المساعد الصغير الكامل
     ↓
🏆 يمكنك بناء مساعد إنتاجي
```

---

## 🚀 الخطوات التالية

### بعد السكريبت 09 - اختر طريقك

**الطريق 1: العمق (البحث)**
- دراسة البنى المتقدمة (Llama 2، Mistral)
- تنفيذ آليات مخصصة (sparse attention)
- المساهمة في أطر العمل (HuggingFace، LlamaIndex)

**الطريق 2: الاتساع (الإنتاج)**
- نشر أنظمة في الإنتاج
- دمج نماذج اللغة الكبيرة (OpenAI، Claude، Ollama)
- إنشاء واجهات (ويب، موبايل، CLI)

**الطريق 3: التطبيق (المجال)**
- التكيف مع صناعتك (صحة، مالية، قانون)
- إنشاء حالات استخدام متخصصة
- تقييم الأداء التجاري

---

## 📖 مراجع سريعة

| ماذا؟ | أين؟ |
|-------|------|
| تثبيت سريع | QUICKSTART_SCRIPT_09.md |
| البنية الكاملة | README.md |
| كود الكتاب | الفصول 1-15 (llm-fr/) |
| التنفيذ | examples/ (السكريبتات 01-09) |
| أطر العمل | الملحق A: موارد متقدمة |

---

**🎉 مبروك! لقد أكملت الكتاب بأكمله وأتقنت المفاهيم الأساسية لنماذج اللغة الكبيرة الحديثة.**

الآن، حان دورك للإبداع! 🚀
