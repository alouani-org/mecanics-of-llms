# البدء السريع: السكريبت 09 - المساعد الصغير الكامل

🌍 [English](../en/QUICKSTART_SCRIPT_09.md) | 📖 [Français](../fr/QUICKSTART_SCRIPT_09.md) | 🇪🇸 [Español](../es/QUICKSTART_SCRIPT_09.md) | 🇧🇷 [Português](../pt/QUICKSTART_SCRIPT_09.md) | 🇸🇦 **العربية**

## 🏆 ما هذا السكريبت؟

**السكريبت 09** هو **المشروع النهائي المتكامل** الذي يجمع **جميع المفاهيم من الفصول 11-15** من الكتاب في نظام متماسك وعملي.

يُظهر كيف يعمل **مساعد مستقل حديث** حقيقيًا:
- ✅ البحث في قاعدة معرفة (RAG)
- ✅ التفكير قبل التصرف (Thought)
- ✅ استخدام أدوات خارجية (حسابات، بحث، ساعة)
- ✅ التكرار حتى إيجاد إجابة
- ✅ تقييم جودته الخاصة
- ✅ اختبار اتساقه (Self-consistency)

## 🚀 التثبيت والتشغيل

### 1. تثبيت التبعيات

```bash
pip install numpy scikit-learn
```

> **ملاحظة**: هذا كل ما تحتاجه للوضع المستقل!
> السكريبتات المكافأة ودمج LLM تتطلب حزمًا إضافية.

### 2. تشغيل السكريبت

```bash
python 09_mini_assistant_complet.py
```

### 3. مشاهدة العرض التوضيحي

سيقوم السكريبت بـ:
1. إنشاء قاعدة معرفة مع 5 وثائق
2. توليد وكيل مع 4 أدوات متاحة
3. طرح 3 أسئلة اختبار
4. تقييم الإجابات
5. اختبار الاتساق (self-consistency)
6. عرض تقرير نهائي

## 📊 فهم المخرجات

### المرحلة 1: قاعدة المعرفة

```
✓ تم إنشاء الفهرس: تمت فهرسة 5 وثائق
```

قام النظام بفهرسة 5 وثائق تعليمية عن:
- المحولات (Transformers)
- RAG
- الوكلاء المستقلون
- تقييم نماذج اللغة الكبيرة
- LoRA و QLoRA

### المرحلة 2: إنشاء الوكيل

```
✓ تم إنشاء الوكيل مع 4 أدوات
```

يمكن للوكيل استخدام 4 أدوات:
1. **calculator**: الحسابات الرياضية
2. **search**: البحث في القاعدة
3. **current_time**: ساعة النظام
4. **summarize**: تلخيص النص

### المرحلة 3: معالجة الأسئلة

```
🤖 السؤال: ما هو المحول (Transformer)؟

⏳ التكرار 1/3
💭 الفكرة: يجب أن أبحث عن معلومات حول المحول
🔧 الفعل: search(query=transformer)
📊 الملاحظة: تم العثور على وثائق: [بنية المحول] (النتيجة: 0.89)

⏳ التكرار 2/3
💭 الفكرة: لقد وجدت معلومات ذات صلة حول المحول.
✅ الإجابة النهائية: الوثائق ذات الصلة تشرح...
```

الوكيل:
1. **يفكر**: يصيغ نيته
2. **يتصرف**: ينفذ أداة
3. **يلاحظ**: يتلقى النتيجة
4. **يكرر** أو **يجيب**: إذا كانت الثقة كافية

### المرحلة 4: تقرير التقييم

```
السؤال 1: ما هو المحول (Transformer)؟...
  • التكرارات: 2
  • الثقة: 100.00%
  • النجاح: ✅

📈 الإحصائيات العامة
  • عدد الأسئلة: 3
  • متوسط التكرارات: 2.0
  • متوسط الثقة: 100.00%
  • معدل النجاح: 100.00%
```

المقاييس المقيّمة (الفصلان 12 و 15):
- **التكرارات**: عدد الخطوات للإجابة (الكفاءة)
- **الثقة**: نقاط الموثوقية بناءً على الأدوات
- **النجاح**: هل وجد النظام إجابة؟

### المرحلة 5: اختبار الاتساق الذاتي

```
اختبار الاتساق الذاتي (3 عينات)

النتائج:
  • الإجابة الأكثر شيوعًا: الوثائق ذات الصلة...
  • نقاط الاتساق: 100.00%
  • إجابات فريدة: 1/3
```

**الاتساق الذاتي** (الفصل 12): مفهوم حيث نطرح **نفس السؤال عدة مرات** ونقيس ما إذا كان النموذج يعطي إجابات مشابهة.

النقاط = `الإجابات المتطابقة / عدد المحاولات`
- 100% = متسق جدًا (مستقر)
- 50% = تم اكتشاف غموض
- 0% = غير متسق جدًا (إشكالي)

## 🔗 المطابقة مع الكتاب

| المفهوم | الفصل | موضح بواسطة | سطر الكود |
|---------|----------|-------------|--------------|
| **المطالبة** | 11 | هيكلة "الفكرة ← الفعل" | `_simulate_llm_reasoning()` |
| **التقييم** | 12 | `AssistantEvaluator` + المقاييس | `evaluate_response()`، `self_consistency_check()` |
| **RAG** | 13 | `RAGSystem` + TF-IDF + التشابه الجيبي | `retrieve()`، `RAGSystem` |
| **وكلاء ReAct** | 14 | `ReActAgent` مع الحلقة الكاملة | `run()` |
| **الإنتاج** | 15 | التكامل + التقييم + معالجة الأخطاء | `main()` |

## 💡 التوسعات المقترحة

### المستوى 1: سهل (30 دقيقة)

1. **تغيير أسئلة الاختبار**
   ```python
   test_questions = [
       "كيف يعمل الانتباه؟",
       "ما هي تكلفة المحول؟",
   ]
   ```

2. **إضافة أداة جديدة**
   ```python
   def tool_weather(city: str) -> str:
       return f"طقس {city}: مشمس، 22°C"
   
   agent.tools.register("weather", "طقس المدينة", tool_weather)
   ```

3. **إضافة المزيد من الوثائق**
   ```python
   rag.add_document("وثيقة جديدة عن...", {"title": "موضوعي"})
   ```

### المستوى 2: متوسط (1-2 ساعة)

4. **دمج نموذج لغة كبيرة حقيقي**
   ```python
   # استبدال _simulate_llm_reasoning() بـ:
   from openai import OpenAI
   client = OpenAI()
   response = client.chat.completions.create(
       model="gpt-4",
       messages=[{"role": "user", "content": prompt}]
   )
   return response.choices[0].message.content
   ```

5. **استمرار المحادثات**
   ```python
   import json
   
   def save_conversation(agent_responses):
       with open("conversations.json", "w") as f:
           json.dump(agent_responses, f, indent=2)
   ```

6. **تحسين RAG**
   - استبدال TF-IDF بتضمينات كثيفة (OpenAI، HuggingFace)
   - استخدام قاعدة بيانات شعاعية (Pinecone، Weaviate، ChromaDB)
   - تنفيذ البحث الهجين (BM25 + Vector)

### المستوى 3: متقدم (2-4 ساعات)

7. **إنشاء واجهة ويب**
   ```bash
   pip install streamlit
   # إنشاء app_agent.py
   ```

8. **النشر في الإنتاج**
   - Docker + FastAPI
   - المراقبة مع Prometheus
   - تسجيل منظم (JSON)

9. **تقييم متقدم**
   - معيار ضد مجموعة اختبار مُعلّمة
   - حساب ROUGE، BERTScore
   - تحليل الهلاوس

## 📝 النمذجة الداخلية

### البنية العامة

```
                    ┌─────────────────────┐
                    │  المستخدم          │
                    │  السؤال            │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │   ReActAgent        │
                    │ (الحلقة المستقلة)   │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │  محاكي LLM          │
                    │  (أو OpenAI/Claude) │
                    └──────────┬──────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
   ┌────▼─────┐         ┌──────▼──────┐      ┌───────▼────┐
   │ RAGSystem │         │ ToolRegistry │      │  Evaluator │
   │ (الفهرسة) │         │ (4 أدوات)    │      │ (المقاييس) │
   └──────────┘         └──────────────┘      └────────────┘
```

### تدفق التنفيذ

```
1. سؤال من المستخدم
2. ReActAgent.run(question)
   a. استدعاء LLM ← "الفكرة + الفعل"
   b. تحليل الفعل (tool_name، params)
   c. ToolRegistry.execute(tool_name, params)
   d. إرجاع الملاحظة
   e. الحلقة: كرر حتى "الإجابة النهائية"
3. إرجاع الإجابة + التاريخ
4. Evaluator.evaluate_response() ← المقاييس
5. عرض التقرير
```

## 🐛 استكشاف الأخطاء

### "ModuleNotFoundError: numpy"
```bash
pip install numpy scikit-learn
```

### الوكيل يدور دون توقف
→ اضبط `max_iterations` عند `agent.run()`:
```python
response = agent.run(question, max_iterations=3)  # 3 محاولات كحد أقصى
```

### الإجابات دائمًا "مستحيل الإجابة"
→ تحقق من فهرسة الوثائق:
```python
print(rag_system.documents)  # يجب أن يعرض 5 وثائق
```

### نقاط الثقة منخفضة جدًا
→ هذا طبيعي في وضع المحاكاة! ادمج نموذج لغة كبيرة حقيقي للحصول على نتائج أفضل.

## 📖 الفصل موضح بالكامل

يُوضح هذا السكريبت **جميع المفاهيم الأساسية** من الفصول 11-15:

| المفهوم | أين؟ |
|---------|------|
| سلسلة التفكير (الفصل 11) | نمط "الفكرة ← الفعل ← الملاحظة" |
| درجة الحرارة والعينات | مُحاكى في `_simulate_llm_reasoning()` |
| Pass@k والتقييم (الفصل 12) | `_calculate_confidence()` |
| الاتساق الذاتي | `self_consistency_check()` |
| RAG (الفصل 13) | `RAGSystem` كاملة + الاسترجاع |
| الوكلاء (الفصل 14) | `ReActAgent` + استدعاء الأدوات |
| الإنتاج (الفصل 15) | معالجة الأخطاء، المراقبة، تسجيل السجلات |

## 🎯 أهداف التعلم

بعد تشغيل وفهم هذا السكريبت، ستكون قادرًا على:

✅ شرح حلقة ReAct (التفكير ← التصرف)
✅ فهم كيفية تكامل الأدوات مع الوكلاء
✅ قياس جودة مساعد (الثقة، التكرارات، الاتساق)
✅ تنفيذ RAG صغير (فهرسة + استرجاع)
✅ تقييم النماذج باستخدام Pass@k والاتساق الذاتي
✅ تكييف الكود لدمج OpenAI، Claude، أو نموذج محلي
✅ تصميم التوسعات (أدوات جديدة، واجهة ويب، إلخ)

---

**تعلم سعيد! 🚀**

للذهاب أبعد ← راجع [INDEX_SCRIPT_09.md](INDEX_SCRIPT_09.md) والفصول 11-15 من الكتاب.
