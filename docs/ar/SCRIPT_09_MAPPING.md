# 🔗 ربط الكود ↔ المفهوم: السكريبت 09

🌍 [English](../en/SCRIPT_09_MAPPING.md) | 📖 [Français](../fr/SCRIPT_09_MAPPING.md) | 🇪🇸 [Español](../es/SCRIPT_09_MAPPING.md) | 🇧🇷 [Português](../pt/SCRIPT_09_MAPPING.md) | 🇸🇦 **العربية**

## 📍 التنقل السريع

- **📖 اقرأ أولاً:** [PEDAGOGICAL_JOURNEY.md](./PEDAGOGICAL_JOURNEY.md) - النظرية
- **⚡ البدء السريع:** [QUICKSTART_SCRIPT_09.md](./QUICKSTART_SCRIPT_09.md) - التشغيل
- **🏗️ البنية:** [INDEX_SCRIPT_09.md](./INDEX_SCRIPT_09.md) - الهيكل

---

## 🎯 ربط الفصول

### الفصل 11: استراتيجيات التوليد والمطالبة (Prompting)

**أين في السكريبت 09؟** ← الدالة `_simulate_llm_reasoning()`

**مفهوم الكتاب:**
- Zero-shot مقابل Few-shot مقابل Chain-of-Thought
- درجة الحرارة والعينات
- بناء المطالبة (prompt)

**كود السكريبت:**
```python
def _simulate_llm_reasoning(query, context):
    """محاكاة استدلال نموذج اللغة الكبيرة"""
    
    # بناء المطالبة (prompting)
    prompt = f"""
    السياق: {context}
    السؤال: {query}
    
    الفكرة (فكر أولاً):
    """
    
    # النظام يولد إجابة
    # درجة الحرارة: التحكم في الإبداع
```

**ما ستتعلمه:**
- بنية المطالبة
- Chain-of-Thought في العمل
- تأثير السياق على الإجابة

---

### الفصل 12: نماذج الاستدلال والتقييم

**أين في السكريبت 09؟** ← الصنف `AssistantEvaluator` والمقاييس

**مفهوم الكتاب:**
- Pass@k: احتمال نجاح واحد على الأقل في k محاولة
- الاتساق الذاتي (Self-consistency): اتساق الإجابات المتعددة
- الثقة ومقاييس الجودة

**كود السكريبت:**
```python
class AssistantEvaluator:
    def evaluate_response(self, question, response, context):
        """تقييم جودة الإجابة"""
        # حساب عدة مقاييس:
        # - الطول
        # - تغطية السياق
        # - الصلة
        # - الاتساق
        
        # إرجاع نقاط 0-100
        return {"score": 78, "metrics": {...}}
    
    def self_consistency_check(self, agent, question, num_samples=3):
        """اختبار ما إذا كان الوكيل يجيب دائمًا بنفس الطريقة"""
        answers = [agent.run(question) for _ in range(num_samples)]
        # القياس: كم مرة نفس الإجابة؟
        consistency_score = self.measure_consistency(answers)
        return {"consistency": 0.85}
```

**ما ستتعلمه:**
- التقييم متعدد المعايير
- الاتساق الذاتي عمليًا
- قياس الجودة

---

### الفصل 13: الأنظمة المعززة وRAG

**أين في السكريبت 09؟** ← الصنف `RAGSystem`

**مفهوم الكتاب:**
- التوليد المعزز بالاسترجاع (Retrieval-Augmented Generation)
- الفهرسة الشعاعية
- استرجاع Top-k

**كود السكريبت:**
```python
class RAGSystem:
    def __init__(self):
        self.documents = {}
        self.index = {}
    
    def add_document(self, text, metadata):
        """إضافة وثيقة إلى القاعدة"""
        doc_id = f"doc_{len(self.documents)}"
        self.documents[doc_id] = {"text": text, "meta": metadata}
    
    def index_documents(self):
        """فهرسة جميع الوثائق"""
        # إنشاء تضمينات بسيطة
        # تخزين للاسترجاع السريع
    
    def retrieve(self, query, top_k=3):
        """استرجاع أعلى k وثيقة ذات صلة"""
        # 1. تضمين الاستعلام
        # 2. التشابه الجيبي مع جميع الوثائق
        # 3. إرجاع top-k
        
        return [
            {"doc_id": "doc_1", "score": 0.89, "text": "..."},
            ...
        ]
```

**ما ستتعلمه:**
- بنية RAG الكاملة
- الفهرسة العملية
- الاسترجاع والترتيب

---

### الفصل 14: بروتوكولات الوكلاء (ReAct)

**أين في السكريبت 09؟** ← الصنف `ReActAgent`

**مفهوم الكتاب:**
- نمط ReAct: التفكير (Reasoning) + التصرف (Acting)
- الحلقة المستقلة
- استدعاء الأدوات وتسجيلها

**كود السكريبت:**
```python
class ReActAgent:
    def __init__(self, rag_system):
        self.rag = rag_system
        self.tools = ToolRegistry()
        self.max_iterations = 3
    
    def run(self, query):
        """تنفيذ حلقة ReAct"""
        for iteration in range(self.max_iterations):
            # THINK: الاستدلال
            thought = self._generate_thought(query, context)
            
            # ACT: اختيار أداة
            tool_name, params = self._parse_action(thought)
            
            # تنفيذ الأداة
            if tool_name in self.tools.registry:
                observation = self.tools.execute(tool_name, params)
            
            # التحقق من الانتهاء
            if "Final Answer" in thought:
                return {"response": thought, "iterations": iteration}
            
            # وإلا، متابعة الحلقة
```

**ما ستتعلمه:**
- الحلقة المستقلة الكاملة
- اختيار الأدوات
- التكرار حتى التقارب

---

### الفصل 15: النشر في الإنتاج

**أين في السكريبت 09؟** ← معالجة الأخطاء، التكامل، المراقبة

**مفهوم الكتاب:**
- معالجة أخطاء قوية
- التسجيل والمراقبة
- التقييم المستمر

**كود السكريبت:**
```python
def main():
    """التنسيق الكامل"""
    try:
        # 1. تهيئة RAG
        rag = RAGSystem()
        
        # 2. تحميل الوثائق
        # 3. الفهرسة
        
        # 4. إنشاء الوكيل
        agent = ReActAgent(rag_system=rag)
        
        # 5. تسجيل الأدوات
        agent.tools.register("calculator", "حسابات", tool_calculator)
        
        # 6. معالجة الأسئلة
        for question in questions:
            try:
                response = agent.run(question)
                # 7. التقييم
                metrics = evaluator.evaluate_response(...)
                # 8. تسجيل النتائج
                log_result(response, metrics)
            except Exception as e:
                handle_error(e)
                continue
    
    except Exception as e:
        log_error(e)
        return False
```

**ما ستتعلمه:**
- تنسيق نظام كامل
- معالجة أخطاء قوية
- المراقبة والتسجيل

---

## 📊 جدول التلخيص

| الفصل | المفهوم | الصنف/الدالة | السطر الأساسي |
|----------|---------|-----------------|-----------|
| 11 | المطالبة | `_simulate_llm_reasoning()` | بناء المطالبة |
| 12 | التقييم | `AssistantEvaluator` | `.evaluate_response()` |
| 13 | RAG | `RAGSystem` | `.retrieve()` |
| 14 | الوكلاء | `ReActAgent` | `.run()` |
| 15 | الإنتاج | `main()` | Try-catch + التسجيل |

---

## 🎯 كيفية دراسة هذا الربط

### الطريقة 1: فصل بفصل
1. اقرأ الفصل من الكتاب
2. تعال لترى القسم المقابل هنا
3. ابحث عن الكود في السكريبت
4. قم بالتشغيل والملاحظة

### الطريقة 2: الكود أولاً
1. افتح `09_mini_assistant_complet.py`
2. اقرأ دالة
3. ابحث هنا عن السياق
4. أعد قراءة الفصل المقابل

### الطريقة 3: سؤال التصحيح
1. لديك سؤال تصحيح
2. ابحث عن الفصل ذي الصلة هنا
3. راجع الكود والمفهوم
4. افهم وصحح

---

## 🔍 فهرس المفاهيم

### A - B - C

- **الوكيل المستقل** ← الفصل 14 (الوكلاء)
- **الانتباه** ← الفصل 3، السكريبت 02
- **قاعدة المعرفة** ← الفصل 13 (RAG)
- **Beam Search** ← الفصل 11 (التوليد)
- **BLEU** ← الفصل 12 (التقييم)
- **BPE** ← الفصل 2، السكريبت 01
- **المعايرة** ← الفصل 11 (المطالبة)
- **Chain-of-Thought** ← الفصل 11، السكريبت 09

### D - E - F

- **DPO** ← الفصل 6
- **التضمينات (Embeddings)** ← الفصل 2، السكريبت 01
- **التقييم** ← الفصل 12، السكريبت 05، 09
- **Few-shot** ← الفصل 11، السكريبت 09
- **الضبط الدقيق** ← الفصل 9، السكريبت 08
- **استدعاء الدوال** ← الفصل 14، السكريبت 06، 09

### G - H - I

- **التوليد** ← الفصل 11، السكريبت 03، 09
- **التأريض (Grounding)** ← الفصل 13 (RAG)
- **المعاملات الفائقة** ← الفصل 11 (درجة الحرارة، Top-k)
- **الفهرسة** ← الفصل 13 (RAG)

### L - M - O

- **LlamaIndex** ← السكريبت 07
- **LoRA** ← الفصل 9، السكريبت 08
- **الخسارة (Loss)** ← الفصل 7
- **MCP** ← الفصل 14 (بروتوكول سياق النموذج)
- **الانتباه متعدد الرؤوس** ← الفصل 3، السكريبت 02
- **الملاحظة (ReAct)** ← الفصل 14، السكريبت 06، 09

### P - Q - R

- **Pass@k** ← الفصل 12، السكريبت 05، 09
- **الحيرة (Perplexity)** ← الفصل 7
- **المطالبة (Prompting)** ← الفصل 11، السكريبت 09
- **QLoRA** ← الفصل 9، السكريبت 08
- **RAG** ← الفصل 13، السكريبت 04، 07، 09
- **ReAct** ← الفصل 14، السكريبت 06، 09
- **الاسترجاع (Retrieval)** ← الفصل 13، السكريبت 04، 07، 09
- **RLHF** ← الفصل 6
- **ROUGE** ← الفصل 12

### S - T - Z

- **الانتباه الذاتي** ← الفصل 3، السكريبت 02
- **الاتساق الذاتي** ← الفصل 12، السكريبت 05، 09
- **Softmax** ← الفصل 3، السكريبت 03
- **درجة الحرارة (Temperature)** ← الفصل 11، السكريبت 03، 09
- **التجزئة (Tokenization)** ← الفصل 2، السكريبت 01
- **استدعاء الأدوات** ← الفصل 14، السكريبت 06، 09
- **Top-k / Top-p** ← الفصل 11
- **الشعاعية (Vectorization)** ← الفصل 13 (RAG)
- **Zero-shot** ← الفصل 11، السكريبت 09

---

## 💡 نصائح القراءة

1. **مبتدئ**: اقرأ بالترتيب من الفصل 11 ← 15
2. **متوسط**: ابدأ بالكود، ثم راجع هنا
3. **متقدم**: عدّل الكود وتحقق من فهمك

---

**تعلم سعيد! 🎓**
